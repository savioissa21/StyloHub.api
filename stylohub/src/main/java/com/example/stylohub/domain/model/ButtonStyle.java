package com.example.stylohub.domain.model;

public enum ButtonStyle {
    ROUNDED,      // Cantos arredondados normais
    PILL,         // Formato de comprimido (totalmente redondo nas pontas)
    SQUARED,      // Quadrado
    OUTLINE,      // Apenas borda, sem fundo
    HARD_SHADOW   // Estilo retro/brutalista (sombra dura)
}